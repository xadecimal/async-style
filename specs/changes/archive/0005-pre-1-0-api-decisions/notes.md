# Notes

## Origin

These decisions began as entries in `specs/future-ideas.md` rather than as one
focused RFC. They were already implemented or resolved before the repository
adopted the spec-driven layout. The archived RFC is therefore a retrospective
record, not evidence that the decisions passed through the current approval
workflow.

The execution option-map reservation landed in commit `af8457b`. The timing and
Promise-combinator documentation decisions were completed with change 0003.

## Archive Normalization

During the spec-driven layout migration on 2026-07-20, the former completed
future-ideas archive document was converted into this complete change
directory. Its still-unresolved follow-ups remain in `specs/future-ideas.md`.
