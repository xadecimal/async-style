# Tasks

## Implementation

- [x] Remove implicit trailing handler parsing from `async`.
- [x] Remove implicit trailing handler parsing from `blocking` and `compute`.
- [x] Remove implicit trailing handler parsing from `async-generator`.
- [x] Remove implicit trailing handler parsing from `await` and `wait`.
- [x] Update examples and documentation to use explicit Clojure `try` forms.
- [x] Update behavioral tests and generated public API documentation.
- [x] Fold the implemented behavior into `specs/current`.
- [x] Preserve the completed change in the archive.
